/**
 * ⚠️ FILE INI PERLU DILENGKAPI
 *
 * Authentication Middleware
 * Verify JWT tokens
 */

// TODO: Import jsonwebtoken

// TODO: Implement authenticateToken middleware
/*
function authenticateToken(req, res, next) {
  // 1. Get token dari Authorization header (format: "Bearer TOKEN")
  // 2. Check if token exists
  // 3. Verify token dengan jwt.verify()
  // 4. Attach decoded user ke req.user
  // 5. Handle errors (expired, invalid)
  // 6. Call next() kalau valid
}
*/

// TODO: Export
// module.exports = { authenticateToken };
