/**
 * ⚠️ FILE INI PERLU DILENGKAPI
 *
 * Authorization Middleware (RBAC)
 * Check user roles dan permissions
 */

// TODO: Implement authorizeRole middleware
/*
function authorizeRole(...allowedRoles) {
  return (req, res, next) => {
    // 1. Check if user authenticated (req.user exists)
    // 2. Check if user.role in allowedRoles array
    // 3. Return 403 kalau not allowed
    // 4. Call next() kalau allowed
  };
}
*/

// TODO: Export
// module.exports = { authorizeRole };
